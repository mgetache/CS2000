// the value of true is Likes Pizza, the value of false is doesn't like pizza.
// if we put likespizza in cout 1 or 0 will be displayed
#include <iostream>
#include<string>
using namespace std;
int main()
{
	bool likesPizza = true;
	
		
	if (likesPizza == true)
	{
		cout<< "Likes pizza";
	}
	else
	{
		cout<<" Doesn't like pizza";
	}
	
	while (true) {}
	return 0;
}
